---
interface Props {
  label: string;
  title: string;
  description: string;
}

const { label, title, description } = Astro.props;
---

<div class="empty-state">
  <span aria-hidden="true">{label}</span>
  <h2>{title}</h2>
  <p>{description}</p>
</div>
